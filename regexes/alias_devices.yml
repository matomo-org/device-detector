# AppleWebKit
- regex: 'Android (?:[\d\.]+;)\s?(?:[^;]+;)?\s?(.+) Build/(?:.+)\) AppleWebKit'
  name: '$1'

- regex: 'Android (?:[\d.]+;)\s?(?:[^;]+;)?\s?(.+)\) AppleWebKit'
  name: '$1'

# UCWEB
- regex: 'Adr [\d\.]+; (?:[a-z]{2}(?:-[a-z]{2})?; )(.+)\) U2/(?:[\d\.]+) UCBrowser'
  name: '$1'